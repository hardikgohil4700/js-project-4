function calculateTriangleArea(base.height){
    return(base * height) / 2;
}

const base = 18;
const height = 18;
constarea = calculateTriangleArea(base,height);
console.log("triangle with base", base, "height", height, "is", area);