function calculateRectangleArea(length,height){
    return length * height;
}

const length = 55;
const height = 80;
const area = calculateRectangleArea(length, height);
console.log("rectangle with length",length, "and height",height, "is", area); 